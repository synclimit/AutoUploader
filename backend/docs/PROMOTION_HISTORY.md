# Promotion History

Every single time the Production Promotion Engine swaps an alias, a `PromotionHistory` object is immutably appended to a permanent ledger. It tracks the exact Operator, the Source Experiment ID, the Previous Version, and the New Version, guaranteeing 100% auditability for all AI behavior changes.
