# Benchmark Runner

The Benchmark Runner pushes Golden Datasets (e.g., 100 known-good Keywords) through the patched sandbox pipeline. It captures core runtime performance (pass, fail, warning, latency) independently of the baseline regression check.
