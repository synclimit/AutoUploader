# Regression Engine

The Regression Engine compares the output of the Sandbox Benchmark run against the static baseline of current production metrics. It surfaces improvements and degradations, mapping changes precisely to determine if a patch violated `promotion_requirements`.
