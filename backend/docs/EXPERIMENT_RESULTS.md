# Experiment Result

The `ExperimentResult` is the immutable ledger of an experiment. It acts as the ultimate determining artifact that decides if an optimization candidate passes systemic muster. If it succeeds, the candidate awaits human Promotion into production.
