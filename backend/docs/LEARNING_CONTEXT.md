# Learning Context

The `LearningContext` encapsulates a single execution of the LearningBuilder. It contains the profile version, the number of feedback packages analyzed, and the generated array of `LearningReport`s.
