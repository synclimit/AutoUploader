# Decision Telemetry

The Decision Engine integrates fully into the immutable `EventBus`.

Events:
1. `DecisionStarted`
2. `DecisionProfileLoaded`
3. `CandidateRanked`
4. `WinnerSelected`
5. `DecisionCompleted`
