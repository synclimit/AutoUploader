# System Health

The `HealthChecker` performs rapid validation of active states: Registry Integrity, Manifest Integrity, Alias Integrity, Telemetry Integrity, Architecture Integrity, Sandbox Integrity, and Configuration Integrity.
