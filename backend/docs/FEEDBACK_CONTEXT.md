# Feedback Context

`FeedbackContext` tracks the immediate sequence of actions that occurred during a given user flow. It is generated by observing `EventBus` and Database updates.

*   Records `session_id`, `candidate_id`, `winner`.
*   Records `manual_edits` and strictly computed `edit_distance`.
*   Records `user_override`.
*   Records real `performance_status`.
