# Dependency Graph

The auto-generated dependency artifacts (JSON, MMD, MD) outline the explicit structural flow required to initialize the OS. No engine can be initialized out of order without violating the Pipeline contract.
