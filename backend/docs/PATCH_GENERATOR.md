# Patch Generator

The Patch Generator produces `ChangePatch` definitions. It defines literal string replacements and JSON manipulations (e.g. `title_length: 70 -> 58`) completely virtually. It does not touch files on the filesystem.
