# Migration Planner

The Migration Planner virtualizes the steps required to transition an active registry component to its experimental state, explicitly delineating source versions, destination versions, and precise operation orders (e.g. Copy -> Patch -> Update Manifest).
